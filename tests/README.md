# tests

Add unit tests for:
1. CSV schema parsing (`src/artextract/data/wikiart.py`)
2. Multi-task row intersection logic (`src/artextract/data/multitask.py`)
3. Metric calculations (`src/artextract/training/metrics.py`)
4. Outlier selection behavior (`src/artextract/training/outliers.py`)
